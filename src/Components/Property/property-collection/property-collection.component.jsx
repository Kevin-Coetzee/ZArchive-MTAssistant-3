import React from "react";

import "./property-collection.styles.scss";

const PropertyCollection = ({ children }) => {
  return <div class="flex-grid">{children}</div>;
};

export default PropertyCollection;
